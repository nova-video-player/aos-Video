<?xml version="1.0" encoding="utf-8"?>
<!--
  Dolby Vision tone-map target luminance dialog: slider + exact text box +
  Auto (display-reported) checkbox.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp">

        <!-- framework SeekBar, not material Slider: the preferences screen
             uses an AppCompat theme without the Material Components color
             attributes the Slider needs to inflate (measured crash). -->
        <SeekBar
            android:id="@+id/dv_nits_slider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="990"
            android:progress="300" />

        <EditText
            android:id="@+id/dv_nits_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:hint="@string/pref_dolby_vision_nits_hint"
            android:inputType="number"
            android:maxLines="1" />

        <CheckBox
            android:id="@+id/dv_nits_auto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="8dp"
            android:text="@string/pref_dolby_vision_target_nits_auto" />

        <TextView
            android:id="@+id/dv_nits_auto_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="4dp"
            android:paddingTop="4dp"
            android:textAppearance="?android:attr/textAppearanceSmall" />
    </LinearLayout>
</ScrollView>
