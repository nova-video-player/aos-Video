# Media I/O and buffering in Nova

The earlier overview has been reconciled with the detailed audit in
[the canonical buffering document](../buffer.md).

That document covers playback and scraping paths, protocol buffers, AVOS queues,
advanced settings, stable/lint differences, implemented fixes and validation.
